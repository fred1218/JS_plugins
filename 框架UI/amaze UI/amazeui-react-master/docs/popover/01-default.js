var popoverInstance = (
  <div style={{height: 80}}>
    <Popover placement="right" positionLeft={50} positionTop={50}>
      这一个是默认 Popover
    </Popover>
  </div>
);

React.render(popoverInstance, mountNode);
